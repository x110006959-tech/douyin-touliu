# V0.1 验收记录

验收日期：2026-07-07

## 当前状态

V0.1 已进入冻结阶段。当前产品边界：

- 只采集。
- 只诊断。
- 只生成动作建议。
- 只做人工审批。
- 只记录人工执行。
- 不自动执行任何平台操作。

主库：

- `douyin_subject_diagnosis`

旧库：

- `ai_ad_diagnosis` 只保留，不使用。

## 全量命令结果

已运行并通过：

```bash
corepack pnpm prisma:generate
corepack pnpm exec prisma validate
corepack pnpm exec prisma db push --skip-generate
corepack pnpm typecheck
corepack pnpm test
corepack pnpm build
```

补充说明：

- 初次 `db push` 前，本机 Docker daemon 未运行，Postgres 端口 `5432` 未监听。
- 已启动 Docker Desktop，并使用 `docker compose -p douyin-local-life up -d postgres redis` 启动依赖。
- 之后 `db push --skip-generate` 成功，数据库与 Prisma schema 同步。

## API smoke 结果

自动 smoke：`apps/api/src/decision-flow.test.ts`

覆盖并通过：

1. 注册用户。
2. 登录用户。
3. 创建 workspace。
4. 创建服务商主体项目。
5. 创建采集任务。
6. 上传直播大屏快照。
7. 生成 `NormalizedMetric`。
8. `GET /collection-tasks/:id/metrics` 正常。
9. `POST /collection-tasks/:id/decision-runs` 成功。
10. 生成 `DecisionRun`。
11. 生成 `ActionProposal`。
12. `GET /projects/:id/action-proposals` 正常。
13. `GET /action-proposals/:id` 正常。
14. approve 一个动作建议。
15. observe 一个动作建议。
16. reject 一个动作建议。
17. mark-manual-executed 一个已审批建议。
18. `ApprovalRecord` 有记录。
19. `ExecutionLog` 有记录。
20. `AuditLog` 有记录。
21. API 返回结构符合 `success/data/error`。

运行结果：

```text
apps/api test: Test Files 2 passed
apps/api test: Tests 2 passed
```

## Web 验收结果

已通过浏览器/构建/源码组合验收：

- `/` 可访问，展示主站首页、产品定位、插件采集边界和 Web 工作台闭环。
- `/login` 可访问，可注册并进入 `/dashboard`。
- `/dashboard` 可访问，展示项目入口和 AI 免责声明。
- `/extension` 可访问，说明插件只采集当前页面可见数据、表格、指标文本和允许的 JSON 响应，不自动点击、修改预算、暂停任务、创建计划或提交平台操作。
- `/privacy` 可无需登录访问，说明采集范围、敏感字段脱敏、数据用途和平台操作边界。
- `/terms` 可无需登录访问，说明服务定位、数据授权、服务限制和 AI 诊断免责声明。
- `/projects/new` 可创建服务商主体项目。
- `/projects/[id]` 可查看项目详情，展示服务商主体、操盘主体、合作关系、当前算法。
- `/tasks/[id]` 可查看任务详情，展示运行诊断、运行决策、标准化指标、动作建议。
- `/decision-center` 可查看动作建议列表。
- `/decision-center` 可按状态筛选，`MANUAL_EXECUTED` 筛选后只显示对应状态建议。
- `/action-proposals/[id]` 可查看动作建议详情。
- 待审批状态可 approve / reject / observe。
- `APPROVED` 状态下可 mark-manual-executed。
- `REJECTED` / `OBSERVING` / `MANUAL_EXECUTED` 状态不显示无效按钮。
- AI 诊断与动作建议页面均显示免责声明。

浏览器验收使用的服务商项目与任务：

- 项目：`V0.1 Web Acceptance Seed ...`
- 任务：`V0.1 web acceptance live dashboard`
- 已验证状态流：`PENDING_APPROVAL -> APPROVED -> MANUAL_EXECUTED`，`PENDING_APPROVAL -> OBSERVING`，`PENDING_APPROVAL -> REJECTED`

## Extension 手动验收步骤

自动环境不能打开 `chrome://extensions/`，需本机手动验收。

验收目录：

```text
apps/extension/dist
```

步骤：

1. 打开 Chrome。
2. 进入 `chrome://extensions/`。
3. 打开开发者模式。
4. 点击“加载已解压的扩展程序”。
5. 选择 `apps/extension/dist`。
6. 确认插件加载无 manifest 报错。
7. 打开一个测试页面或已登录的业务后台页面。
8. 打开插件 popup。
9. 配置 API 地址，例如 `http://localhost:4000`。
10. 配置 SaaS token。
11. 点击“开始采集”。
12. 查看本地快照。
13. 确认快照里没有 password、cookie、token、authorization、secret。
14. 确认网络记录最多 50 条。
15. 点击“上传快照”。
16. 到 API / Web 确认 `DataSnapshot` 已生成。
17. 在 Web 任务详情页运行 `DecisionRun`。
18. 确认生成 `ActionProposal`。
19. 完成审批、观察、拒绝和人工执行记录验证。

说明：Extension popup 文案后续单独同步，不作为本次 V0.1 最终验收的通过条件。本次插件验收以 manifest 权限、源码安全边界、脱敏测试、构建产物和 Web 插件承接页为准。

## Extension 自动验证

已通过：

```bash
corepack pnpm --filter @douyin-local-life/extension typecheck
corepack pnpm --filter @douyin-local-life/extension test
corepack pnpm --filter @douyin-local-life/extension build
```

覆盖：

- 递归敏感字段脱敏。
- 非 JSON 响应不记录。
- 非白名单域名不记录。
- 网络记录最多 50 条。
- content/service-worker/injected 源码不包含自动点击、提交、cookies/history/bookmarks/downloads 权限调用。

## 安全边界搜索结果

全文搜索未发现真实平台自动执行实现。

命中项说明：

- `password`：仅用于系统用户注册登录、hash 存储和测试输入，不保存平台密码。
- `token/authorization`：仅用于本系统 SaaS API 鉴权、插件 session 存储和脱敏测试，不采集平台 token。
- `ActionProposal`：只生成建议、审批、观察、人工执行记录，不执行到平台页面。
- `预算/暂停`：属于动作建议文本和规则输出，仍需人工审批。
- `chrome.cookies/history/bookmarks/downloads`：只出现在安全守卫测试中，manifest 未申请这些权限。

## 已知风险

- Chrome 插件 `host_permissions` 仍包含 `*.bytedance.com`，后续拿到真实后台域名后应继续收窄。
- Web 自动化验收依赖当前登录态和本地 API，建议 V0.2 增加正式 E2E 测试。
- Extension popup 文案后续单独同步；当前 V0.1 最终验收不把 popup 文案作为通过条件。
- 部分历史文件曾出现终端显示乱码，已优先修正文档和动作详情页；后续可继续清理旧页面文案。
- 当前 LLM 仍为 mock/provider 框架，V0.1 以规则引擎为准。

## V0.2 建议

- 待校准队列。
- 活动库热更新与后台核验。
- OCR 置信度和人工校准优先级。
- 服务商 SOP、排班、脚本、服务费证据结构化。
- 更细的主体识别置信度来源。
- Web E2E 自动化。
- 插件域名白名单进一步收窄。
