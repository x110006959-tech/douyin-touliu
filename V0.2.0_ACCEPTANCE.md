# V0.2.0 验收记录

验收日期：2026-07-12

## 本次交付

- 固定目标页面串行巡检和采集批次管理。
- 路线完整性、新鲜度、连续失败降级和强动作阻断。
- 动作建议过期、替代、冷却、频控和只读决策预演。
- 业务健康接口、Web 健康中心和 AI 解释熔断。
- Chrome Extension `0.2.0` 发布包，权限保持 `activeTab`、`storage`。

## 安全验收

- 巡检只读取用户已经打开的白名单页面，并且必须由用户主动开始。
- 不自动创建或导航标签页，不点击、不修改预算、不暂停计划、不提交表单。
- 决策预演无业务写入；AI 仅负责解释，确定性规则负责安全判断。
- 过期建议不能审批、拒绝、观察或标记为人工执行。

## 自动验证

- `corepack pnpm exec prisma validate`：通过。
- `corepack pnpm typecheck`：通过。
- 全仓测试：69 项通过。
- `corepack pnpm build`：通过。
- Extension 最终 ZIP 安全回归：通过。
- 浏览器验收：仪表盘健康中心、决策中心新状态、任务页路线质量和预演入口渲染正常，控制台无错误或警告。

## 环境说明

- API 集成测试在隔离 Docker 网络内完成，临时容器、网络、镜像和测试卷已清理。
- 未重启 Docker Desktop。
- 本次未部署腾讯云 staging，未修改 DNS 或正式流量。

## staging 待验收

1. 应用数据库 schema 并启动 V0.2.0 Compose。
2. 加载 Extension `0.2.0`，由用户手动打开真实固定目标页面并开始巡检。
3. 校准路线 URL、字段别名、指标单位和时间窗口。
4. 验证缺页、过期、连续失败、建议过期/冷却以及完整人工决策闭环。
