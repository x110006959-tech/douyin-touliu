# V0.1.1 验收记录

验收日期：2026-07-07

## 目标

V0.1.1 新增“数据复核表 + 字段来源标记”。插件采集和后端标准化得到的指标先进入待复核状态，用户可在 Web 任务详情页确认、修改或忽略。DecisionRun 优先使用已复核指标；没有已确认/修改指标时回退 NormalizedMetric，并降低置信度、提示人工复核。

第一版仍不提供任何自动投放执行能力，不自动点击平台页面、不改预算、不暂停任务、不创建计划、不提交平台表单。

## 新增模型和类型

- Prisma enum：`MetricSource`
- Prisma enum：`MetricReviewStatus`
- Prisma model：`ReviewedMetric`
- `NormalizedMetric` 补充：`confidence`、`rawEvidence`
- shared 类型：`ReviewedMetricDTO`、`ReviewMetricInput`、`BulkReviewMetricInput`
- DecisionEngineInput 补充：`dataReviewStatus`、`reviewCoverage`、`metricLayer`

## 新增 API

- `GET /collection-tasks/:id/review-metrics`
- `PATCH /review-metrics/:id`
- `POST /collection-tasks/:id/review-metrics/bulk`
- `POST /collection-tasks/:id/review-metrics/confirm-all`

## DecisionRun 数据选择

1. 有 `CONFIRMED` / `MODIFIED` 的 `ReviewedMetric` 时，使用 ReviewedMetric。
2. `IGNORED` 指标不进入 DecisionRun。
3. 没有可用 ReviewedMetric 时，回退 NormalizedMetric。
4. 回退时 `dataReviewStatus = UNREVIEWED`，confidence 乘以 0.8，并加入“当前数据未经过人工复核”的 manualCheckItems。
5. `DecisionRun.inputJson` 记录 `metricLayer`、`dataReviewStatus`、`reviewCoverage`。

## Web

`/tasks/[id]` 新增“数据复核表”，位于原始快照/标准化指标之后、诊断建议之前。支持：

- 查看指标名、key、采集值、确认值、单位、来源、置信度、页面类型、口径、时间范围、原始证据、复核状态。
- 单条确认、修改、忽略。
- 一键确认全部。
- 保存修改。
- 刷新复核指标。
- 决策按钮区域显示数据状态：已复核、未完全复核、无指标。

## AuditLog

已覆盖：

- `REVIEW_METRICS_INITIALIZED`
- `REVIEW_METRIC_UPDATE`
- `REVIEW_METRICS_BULK_UPDATE`
- `REVIEW_METRICS_CONFIRM_ALL`
- `DECISION_RUN_USE_REVIEWED_METRICS`
- `DECISION_RUN_FALLBACK_NORMALIZED_METRICS`

## 验证结果

已运行并通过：

```bash
corepack pnpm exec prisma validate
corepack pnpm exec prisma db push --skip-generate
corepack pnpm typecheck
corepack pnpm test
corepack pnpm build
corepack pnpm --filter @douyin-local-life/web typecheck
```

补充测试覆盖：

1. NormalizedMetric 初始化 ReviewedMetric。
2. 单个指标 CONFIRMED。
3. 单个指标 MODIFIED。
4. 单个指标 IGNORED。
5. 批量复核。
6. 一键确认全部。
7. DecisionRun 优先使用 ReviewedMetric。
8. 没有可用 ReviewedMetric 时回退 NormalizedMetric 并降低 confidence。
9. IGNORED 指标不进入 DecisionRun。
10. AuditLog 有记录。

