# project-001-字节投流

创建日期：2026-07-04

当前状态：已创建标准项目文件夹，开始纳入全局工作台管理。

## 项目信息

- 项目编号：project-001
- 项目名称：字节投流
- 项目文件夹：`10_项目档案/project-001-字节投流`
- 所属 Workspace：`C:\Users\Admin\Documents\字节投流`

## 项目目标

用于承载“字节投流”相关的需求、过程记录、素材资料、交付物和复盘沉淀。

## 资料归档规则

- 需求和目标放入 `01_需求与目标`
- 执行过程放入 `02_执行过程`
- 素材和参考资料放入 `03_素材与资料`
- 最终交付物放入 `04_交付物`
- 项目复盘和经验沉淀放入 `05_复盘与沉淀`

## 重要决策记录

### 2026-07-04

- 当前项目正式采用全局工作台规则。
- 当前项目文件夹命名为 `project-001-字节投流`。
- 后续项目相关内容必须归档到本项目文件夹内。

### 2026-07-12

- V0.2.0 固定页面巡检、采集质量和动作建议生命周期治理已完成本地开发与验收。
- Extension 保持只采集边界，巡检必须由用户主动启动，不导航、不点击、不提交平台页面。
- 当前转入服务器 staging 部署与真实页面路线/字段校准阶段。

### 2026-07-20

- 认证入口策略明确为仅隐藏公开注册和邮箱验证入口，后端注册、邮箱验证和数据模型保留；当前不是服务端邀请制。
- 对抗性审查补充完成持久化输入边界：外部自由输入拒绝敏感凭证形态，内部派生诊断/AI 数据二次脱敏后保存。
- 全仓工程验证通过，共 187 项测试；无数据库 schema、migration、环境变量或部署变更。
- 审计留存 v032 已完成隔离空库和 v031 升级验证；用户删除后审计仅解除外键关联并保留最小操作者快照。运行时和制品 Schema 版本已统一为 v032，191 项全仓测试与生产依赖审计通过。

### 2026-07-24

- v033 完成域名级、一次确认的直连采集和任务校准大屏；`liveboard2` 与 `promotion/roi2` 已有明确路线映射，旧未识别记录的根因是没有触发用户确认采集。
- Popup 手选路线与点击采集现由服务端联合 Extension 凭证、账号任务归属、精确可信 HTTPS 来源和路线证据校验后一次生效；普通网页/手工入口仍不能伪造确认状态，非可信来源会被拒绝。
- 页面可见文本只在当前点击流程内存中用于提取，上传契约和服务端均不保留页面正文；采集证据只保留经过清洗的指标、表格、来源与校准审计。
- 本轮工程验证完成（lint、typecheck、200 项测试、build、Prisma validate/generate、版本和差异检查）；真人登录的页面与大屏验收仍按安全边界由用户手动执行。

### 2026-07-25

- v033 校准大屏的指标汇总按路线保留同名指标，避免不同后台页面中的“消耗”等真实采集值在展示和复核前互相覆盖。
- API 回归在隔离数据库中验证两条路线的同名指标可同时返回；未进行生产 migration、部署、提交或平台操作。

### 2026-07-26

- 修复隔离测试登录页在 API 暂不可用时长期显示“正在确认登录状态…”的问题：首次会话检查使用独立 3 秒上限，失败后显示登录表单；不改变正常登录、HttpOnly Cookie 或 CSRF 初始化策略。
- 已重新构建 `127.0.0.1:3400` 隔离 Web 并连接 `127.0.0.1:4400` 隔离 API，浏览器实测登录表单可见且无控制台错误；Web 23 项测试、typecheck 和 production build 通过。

### 2026-07-27

- 用户确认后，保留原 PostgreSQL 数据卷将 `127.0.0.1:3300/4300` 本地预上线环境升级为 v033。升级前自定义格式备份已恢复到独立临时 PostgreSQL 并验证可用；备份未上传或提交。
- v033 只应用 `TableCellReview` 加法式迁移。升级后原数据环境保留 9 个用户、5 条快照和 5 个采集任务，API/Web/PostgreSQL 均 healthy，登录页加载正常。

### 2026-07-28

- 完成采集协议与原子写入收口：新旧插件/API 不兼容时停止上传，可信快照与标准指标、待复核、路线状态、心跳和批次状态同事务提交。
- 完成幂等历史修复：只补四条 `VERIFIED` 路线缺失的派生记录，首条 `MANUAL_PENDING` 概览保持原样；最终复跑无候选。
- 校准大屏改为深蓝数据驾驶舱，总览按“本地推总览优先、直播概览回退”选择单一来源，不跨路线相加；桌面和移动端视觉回归通过。
- 本地运行版本为 0.2.4，213 项测试及完整工程验证通过；有数据、空数据、桌面与移动端均已验收。本地插件需用户在 Chrome 手动重新加载；生产候选包尚未正式发布。

### 2026-07-30

- v034 完成真实数据准确性与可信度校准：五路线字段白名单、卡片/表格绑定证据、周期和结构指纹门禁、精确数值解析、ROI 交叉校验以及全局失败关闭均已实现。
- 未知表格结构必须逐格核对完整张表后才形成结构校准；同路线、同页面指纹和同表签名的后续稳定快照才可批量放行。
- 校准大屏可审核后台字段标签、后台原值、系统精确值、单位/精度、周期、位置和异常原因；摘要优先使用后台原样文本，百分比内部比例不会伪装成后台百分比。
- 全仓工程验证通过，共 244 项测试；空 PostgreSQL 已验证全部 15 个 migration。真实五路线采集验收仍必须由用户主动完成，未迁移本地业务库、未部署或发布。
