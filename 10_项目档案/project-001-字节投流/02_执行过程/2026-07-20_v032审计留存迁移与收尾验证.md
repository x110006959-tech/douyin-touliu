# 2026-07-20 v032 审计留存迁移与收尾验证

## 目标

- 完成对抗性审查的最后三项收尾：验证 v032 审计迁移、执行全仓门禁、归档实际结果。
- 保持认证策略为仅隐藏公开注册和邮箱验证入口；不删除后端流程，不改为服务端邀请制。

## 实施

- 新增 `20260720110000_v032_audit_actor_snapshot`，将 `AuditLog.userId` 改为可空、增加最小 `{ userId }` 快照，并用 `ON DELETE SET NULL` 代替级联删除。
- 关键复核和执行结果复盘在事务内重新校验业务状态，再与审计原子写入。
- API、Compose、镜像、环境示例和 Extension 构建元数据的默认 Schema 版本统一为 `20260720_v032_audit_actor_snapshot`。

## 验证

- 在隔离 PostgreSQL 空库顺序应用全部 13 个 migration；独立 v031 数据库升级至 v032 均成功。
- 升级夹具确认旧审计记录回填用户 ID 快照；删除用户后审计记录仍存在、关联 ID 为 `null`、快照保留。
- 全仓 `lint`、`typecheck`、`test`、`build`、Prisma validate/generate、生产依赖 audit、带无敏感占位变量的 Compose 静态配置、v032 正式 Extension 制品安全测试和 `git diff --check` 均通过。
- 测试共 191 项：shared 36、extension 28、web 19、llm 6、decision-engine 34、API 68；生产依赖 audit 为 0 个已知漏洞。

## 边界

- 未执行真实 SMTP、COS、部署、平台自动化或生产数据操作。
- 生产迁移前仍需先离机备份，并在 staging 用 `prisma migrate deploy` 和 `/version` 复核 v032。
