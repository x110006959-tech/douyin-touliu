# 直播 API 结构漂移与失败反馈

## 现场事实

- 商品页在 `17:56:59`、`17:57:04` 出现 `SCHEMA_MISMATCH`。
- 本机 API 内部开关为开启状态，但没有收到任何实时脉冲，因此任务大屏没有实时指标。
- 原因位于 Extension 响应校验前，不能用保存正式快照替代实时链路修复。

## 修复原则

- 只兼容明确的等价响应包装、对象新增字段与 `null` 缺失值。
- 只投影既有白名单字段，不保存平台原始响应、未知字段、Cookie、Token 或 Authorization。
- 实时失败必须显示端点和原因，并明确是否已经上传到服务端。

## 验收要点

1. 重载解包插件，确认构建指纹 `ed4b04e82725`。
2. 保持直播平台页可见，在下一个整 5 秒点观察实时帧。
3. 若仍失败，Popup 应显示具体端点且说明未上传；依据该最小证据继续做下一轮白名单适配。

## 2026-08-11 追加：失败停止后的按钮状态

- 截图中的“已停止”与“停止 API 持续采集”并存不是后台继续运行，而是 Popup 轮询遗漏按钮同步。
- 修复后，`GET_STATE.livePulse.active=false` 时统一显示“开始 API 持续采集”；配对未验证或 API 开关关闭时按钮保持禁用。
- 后续职责隔离制品指纹：`c49f72be4e03`。全仓验证已通过，人工验收必须先重载该指纹。

## 2026-08-11 追加：分钟趋势不再阻断实时脉冲

- 根因已精确到 `room_minute_indicator`：旧实时调度在整分钟把它附加到 PULSE；该端点的 Schema 漂移在上传前停止了整个会话，故服务端没有任何实时脉冲。
- 修复不是放宽未知平台结构，而是职责隔离。PULSE 永远只请求 `key_index`，仅发送实时白名单指标；分钟趋势只允许由用户主动正式 SNAPSHOT 获取并投影为 `HOURLY_ROWS`。
- 服务端源码拒绝 PULSE 夹带分钟行或分钟端点状态，Popup 继续显示按任务隔离的具体失败原因。跨整分钟不应再影响实时指标。
- 本机 API 已于 2026-08-11 08:54 原地更新，运行容器确认 PULSE 只允许 `key_index`，`/ready`、`/version` 均为 HTTP 200。本轮未触发真实平台请求、没有写入历史快照或数据库。人工验收：重载 `c49f72be4e03` 后，在平台页保持可见，手动启动一次实时采集，确认 Popup 显示成功次数、`1 个端点`，任务大屏收到实时帧。
